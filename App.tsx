import { LogBox } from 'react-native';

// Ignore the VirtualizedList warning as it's handled properly in our implementation
LogBox.ignoreLogs(['VirtualizedLists should never be nested']);

// ... existing code ... 